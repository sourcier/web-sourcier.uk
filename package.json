{
  "name": "web-sourcier-uk",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -rf .astro dist public/pagefind",
    "dev": "pnpm thumbnails:copy && SHOW_DRAFTS=true netlify dev",
    "dev:no-drafts": "pnpm thumbnails:copy && netlify dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "thumbnails:generate": "node scripts/process-thumbnails.mjs",
    "thumbnails:copy": "node scripts/copy-thumbnails.mjs",
    "search:index": "pnpm thumbnails:copy && astro build && pagefind --site dist && rm -rf public/pagefind && cp -r dist/pagefind public/pagefind"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@expressive-code/plugin-line-numbers": "^0.41.7",
    "@fortawesome/fontawesome-svg-core": "^7.2.0",
    "@fortawesome/free-brands-svg-icons": "^7.2.0",
    "@fortawesome/free-solid-svg-icons": "^7.2.0",
    "astro": "^6.1.1",
    "astro-expressive-code": "^0.41.7",
    "bulma": "^1.0.4",
    "mermaid": "^11.13.0",
    "sass": "^1.98.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@netlify/edge-functions": "^3.0.6",
    "pagefind": "^1.4.0",
    "remark-emoji": "^5.0.2"
  }
}
