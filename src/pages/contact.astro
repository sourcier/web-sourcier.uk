---
import BaseLayout from "../layouts/BaseLayout.astro";
import PageHero from "../components/PageHero.astro";
import MailingListCTA from "../components/MailingListCTA.astro";
import {
  faBriefcase,
  faMicrophone,
  faUserGraduate,
  faSquareCaretDown,
} from "@fortawesome/free-solid-svg-icons";
import { faLinkedin, faGithub } from "@fortawesome/free-brands-svg-icons";
import { faIcon } from "../utils/icons";

const projectsIcon = faIcon(faBriefcase, { size: 24 });
const speakingIcon = faIcon(faMicrophone, { size: 24 });
const mentoringIcon = faIcon(faUserGraduate, { size: 24 });
const linkedinIcon = faIcon(faLinkedin, { size: 20 });
const githubIcon = faIcon(faGithub, { size: 20 });
const [caretWidth, caretHeight, , , caretPathData] = faSquareCaretDown.icon;
const caretPaths = Array.isArray(caretPathData)
  ? caretPathData
  : [caretPathData];
const selectCaretSvg = [
  `<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 ${caretWidth} ${caretHeight}'>`,
  ...caretPaths.map((d) => `<path fill='black' d='${d}'/>`),
  `</svg>`,
].join("");
const selectCaretIcon = `url("data:image/svg+xml,${encodeURIComponent(selectCaretSvg)}")`;
const pageTitle = "Contact — Sourcier";
---

<BaseLayout
  pageTitle={pageTitle}
  description="Get in touch with Roger Rajaratnam about projects, speaking opportunities, or mentoring."
>
  <PageHero
    kicker="Get in touch"
    title="Let's work together"
    subtitle="Open to projects, speaking opportunities, and mentoring conversations."
  />

  <!-- Contact reasons -->
  <div class="contact-reasons">
    <div class="container is-max-desktop">
      <ul class="contact-reasons__list" role="list">
        <li class="contact-reasons__item">
          <span class="contact-reasons__icon-wrap">
            <span class="contact-reasons__icon" set:html={projectsIcon} />
          </span>
          <div>
            <p class="contact-reasons__title">Projects</p>
            <p class="contact-reasons__desc">
              Engineering leadership or full-stack development work.
            </p>
          </div>
        </li>
        <li class="contact-reasons__item">
          <span class="contact-reasons__icon-wrap">
            <span class="contact-reasons__icon" set:html={speakingIcon} />
          </span>
          <div>
            <p class="contact-reasons__title">Speaking</p>
            <p class="contact-reasons__desc">
              Talks on software engineering, technical leadership, or career
              growth.
            </p>
          </div>
        </li>
        <li class="contact-reasons__item">
          <span class="contact-reasons__icon-wrap">
            <span class="contact-reasons__icon" set:html={mentoringIcon} />
          </span>
          <div>
            <p class="contact-reasons__title">Mentoring</p>
            <p class="contact-reasons__desc">
              Supporting engineers looking to grow their skills or break into
              the field.
            </p>
          </div>
        </li>
      </ul>
    </div>
  </div>

  <section class="section">
    <div class="container is-max-desktop">
      <div class="contact-layout">
        <div class="contact-form-col">
          <p class="contact-form__intro">
            Use the form below and I'll get back to you as soon as I can.
          </p>

          <form
            class="contact-form"
            name="contact"
            method="post"
            data-netlify="true"
            netlify-honeypot="bot-field"
          >
            <div class="contact-form__row">
              <div class="contact-form__field">
                <label class="contact-form__label" for="contact-name"
                  >Name</label
                >
                <input
                  class="contact-form__input"
                  id="contact-name"
                  type="text"
                  placeholder="Your name"
                  name="name"
                  required
                />
              </div>
              <div class="contact-form__field">
                <label class="contact-form__label" for="contact-email"
                  >Email</label
                >
                <input
                  class="contact-form__input"
                  id="contact-email"
                  type="email"
                  placeholder="your@email.com"
                  name="email"
                  required
                />
              </div>
            </div>

            <div class="contact-form__field">
              <label class="contact-form__label" for="contact-reason"
                >Reason</label
              >
              <select
                class="contact-form__input"
                id="contact-reason"
                name="reason"
                style={`--contact-select-caret-icon: ${selectCaretIcon}`}
                required
              >
                <option value="" disabled selected>Select a reason…</option>
                <option value="project">Project</option>
                <option value="speaking">Speaking</option>
                <option value="mentoring">Mentoring</option>
                <option value="other">Other</option>
              </select>
            </div>

            <div class="contact-form__field is-hidden" id="other-reason-field">
              <label class="contact-form__label" for="contact-other-reason"
                >Please specify</label
              >
              <input
                class="contact-form__input"
                id="contact-other-reason"
                type="text"
                placeholder="Please specify your reason…"
                name="other-reason"
              />
            </div>

            <div class="contact-form__field">
              <label class="contact-form__label" for="contact-message"
                >Message</label
              >
              <textarea
                class="contact-form__textarea"
                id="contact-message"
                placeholder="Tell me a bit about what you have in mind…"
                name="message"
                rows="6"
                required></textarea>
            </div>

            <div class="field is-hidden" aria-hidden="true">
              <input name="bot-field" type="text" />
            </div>

            <button class="contact-form__submit" type="submit"
              >Send Message &rarr;</button
            >
          </form>

          <div class="contact-success is-hidden" data-success-message>
            <p class="contact-success__text">
              &#10003; Message sent — I'll get back to you soon.
            </p>
            <button class="contact-success__close" type="button">Dismiss</button
            >
          </div>
        </div>

        <aside class="contact-sidebar">
          <h2 class="contact-sidebar__heading">Find me online</h2>
          <div class="contact-sidebar__links">
            <a
              class="contact-sidebar__link"
              href="https://www.linkedin.com/in/roger-rajaratnam"
              target="_blank"
              rel="noopener noreferrer"
            >
              <span class="contact-sidebar__link-label">
                <span
                  class="contact-sidebar__link-icon"
                  set:html={linkedinIcon}
                />
                LinkedIn
              </span>
              <span class="contact-sidebar__link-desc"
                >Software engineering &amp; leadership</span
              >
            </a>
            <a
              class="contact-sidebar__link"
              href="https://github.com/sourcier"
              target="_blank"
              rel="noopener noreferrer"
            >
              <span class="contact-sidebar__link-label">
                <span
                  class="contact-sidebar__link-icon"
                  set:html={githubIcon}
                />
                GitHub
              </span>
              <span class="contact-sidebar__link-desc"
                >Open source &amp; blog code</span
              >
            </a>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <MailingListCTA />
</BaseLayout>

<style lang="scss">
  /* ── Contact reasons bar ───────────────────────────────────── */
  .contact-reasons {
    margin-top: 3rem;
    padding: 0 1.5rem;
  }

  .contact-reasons__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;

    @media (min-width: 640px) {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .contact-reasons__item {
    display: flex;
    gap: 1rem;
    align-items: center;
    padding: 1.5rem 1.25rem;
    background-color: var(--color-surface);
    border: 1px solid var(--color-border);
    border-top: 3px solid var(--color-pink);
    border-radius: 6px;
    transition:
      transform 0.18s ease,
      box-shadow 0.18s ease;

    &:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 24px rgba(232, 0, 106, 0.12);
    }
  }

  @keyframes icon-float {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-5px);
    }
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .contact-reasons__item {
    animation: fade-in-up 0.4s ease both;

    &:nth-child(1) {
      animation-delay: 0ms;
    }
    &:nth-child(2) {
      animation-delay: 80ms;
    }
    &:nth-child(3) {
      animation-delay: 160ms;
    }

    &:hover .contact-reasons__icon-wrap {
      animation: icon-float 0.6s ease infinite;
    }
  }

  .contact-reasons__icon-wrap {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    width: 3rem;
    height: 3rem;
    border-radius: 50%;
    background-color: rgba(232, 0, 106, 0.1);
    transition: background-color 0.18s ease;

    .contact-reasons__item:hover & {
      background-color: rgba(232, 0, 106, 0.18);
    }
  }

  .contact-reasons__icon {
    color: var(--color-pink);
    line-height: 1;
    display: flex;
  }

  .contact-reasons__title {
    font-family: "Barlow Condensed", sans-serif;
    font-size: 1.375rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    color: var(--color-ink);
    margin-bottom: 0.25rem;
  }

  .contact-reasons__desc {
    font-size: 0.9375rem;
    line-height: 1.6;
    color: var(--color-muted);
    margin: 0;
  }

  /* ── Form intro ────────────────────────────────────────────── */
  .contact-form__intro {
    font-size: 0.9375rem;
    color: var(--color-ink);
    margin-bottom: 2rem;
  }

  /* ── Select styling ────────────────────────────────────────── */
  select.contact-form__input {
    appearance: none;
    background-image: var(--contact-select-caret-icon);
    background-repeat: no-repeat;
    background-position: right 1rem center;
    background-size: 0.75rem 0.75rem;
    cursor: pointer;

    &.is-placeholder {
      color: var(--color-muted);
      opacity: 0.6;
    }

    option {
      color: var(--color-ink);
      opacity: 1;
    }
  }
</style>

<script>
  const handleSubmit = (event: Event) => {
    event.preventDefault();

    const form = event.target as HTMLFormElement;
    const formData = new FormData(form);

    fetch("/", {
      method: "POST",
      headers: { "Content-Type": "application/x-www-form-urlencoded" },
      body: new URLSearchParams(
        formData as unknown as Record<string, string>,
      ).toString(),
    })
      .then(() => {
        form.reset();
        form.style.display = "none";
        const successEl = document.querySelector(
          "[data-success-message]",
        ) as HTMLElement | null;
        successEl?.classList.remove("is-hidden");
        successEl?.setAttribute("tabindex", "-1");
        successEl?.focus();
      })
      .catch((error) => alert(error));
  };

  const handleSuccessMessageClose = () => {
    const form = document.querySelector(
      "form[name='contact']",
    ) as HTMLFormElement;
    const successMessage = document.querySelector(
      "[data-success-message]",
    ) as HTMLElement;

    form.style.display = "";
    successMessage.classList.add("is-hidden");
  };

  document
    .querySelector("form[name='contact']")
    ?.addEventListener("submit", handleSubmit);

  const reasonSelect = document.querySelector(
    "#contact-reason",
  ) as HTMLSelectElement | null;
  const otherReasonField = document.querySelector(
    "#other-reason-field",
  ) as HTMLElement | null;
  const otherReasonInput = document.querySelector(
    "#contact-other-reason",
  ) as HTMLInputElement | null;

  reasonSelect?.addEventListener("change", () => {
    const isOther = reasonSelect.value === "other";
    otherReasonField?.classList.toggle("is-hidden", !isOther);
    if (otherReasonInput) {
      otherReasonInput.required = isOther;
    }
    reasonSelect.classList.toggle("is-placeholder", reasonSelect.value === "");
  });

  // Set initial placeholder state
  if (reasonSelect) {
    reasonSelect.classList.add("is-placeholder");
  }

  document
    .querySelector("[data-success-message] .contact-success__close")
    ?.addEventListener("click", handleSuccessMessageClose);
</script>
