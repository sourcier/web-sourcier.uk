---
import BaseLayout from "../layouts/BaseLayout.astro";
import PageHero from "../components/PageHero.astro";
import CourseInterestForm from "../components/CourseInterestForm.astro";
import {
  faClock,
  faLayerGroup,
  faUsers,
} from "@fortawesome/free-solid-svg-icons";
import {
  courseBundle,
  courseLaunchLabel,
  courseOffers,
  courseSupportOffer,
} from "../data/courses";
import { getCourseIcon } from "../utils/contentIcons";
import { faIcon } from "../utils/icons";

const learningModel = [
  {
    title: "Still relevant in the age of AI",
    icon: faIcon(faLayerGroup, { size: 18 }),
    body: "AI can help you move faster, but it cannot replace judgment about semantics, accessibility, architecture, debugging, and delivery. These courses build the context you need to use AI well instead of trusting it blindly.",
  },
  {
    title: "More attention than a boot camp cohort",
    icon: faIcon(faClock, { size: 18 }),
    body: "When I taught at boot camps, one of the hardest things was giving every student the attention they needed at the exact moment they were stuck. The self-paced format plus included 1:1 support is designed to fix that.",
  },
  {
    title: "Smaller upfront commitment",
    icon: faIcon(faUsers, { size: 18 }),
    body: "You can start with one course instead of paying for an all-or-nothing programme, then add more only when it helps.",
  },
];

const supportMoments = [
  "Project reviews when you want grounded feedback on architecture, code, or delivery decisions.",
  "Debugging sessions when you are stuck and need to move faster than a forum thread or recorded lesson can help.",
  "Roadmap resets when you are not sure what to learn next or how to connect isolated topics into real project work.",
];

const supportInclusions = [
  {
    title: "Every route includes 1:1",
    body: "Whether you start with one course or take the full bundle, direct support is part of the offer.",
    icon: faIcon(faUsers, { size: 18 }),
  },
  {
    title: "Self-paced does not mean unsupported",
    body: "Use 1:1 to turn the recorded material into help that fits your pace, questions, and project context.",
    icon: faIcon(faLayerGroup, { size: 18 }),
  },
  {
    title: "Add more if you need it",
    body: "Top up with extra 1:1 sessions when you want more review, debugging, or planning time.",
    icon: faIcon(faClock, { size: 18 }),
  },
];
---

<BaseLayout
  pageTitle="Courses — Sourcier"
  description="Launching soon: self-paced web development courses built for the age of AI, with stronger fundamentals, included 1:1 support, and the option to start with one course or take the full bundle."
>
  <PageHero
    kicker="Courses"
    title="Choose the full bundle or start with one course"
    tone="secondary"
    subtitle="Launching soon: these are the structured learning routes. If you want free, article-led orientation, start with the guides. If you want a connected route through web foundations, frontend, backend, and DevOps with included 1:1 support, start here. Start with one course to keep the cost of entry smaller, or take the full bundle for end-to-end context."
  />

  <section
    class="section courses-bundle flow-section"
    aria-labelledby="courses-bundle-section-heading"
  >
    <div class="container is-max-desktop">
      <div class="courses-bundle__section-header">
        <p class="section-label section-label--secondary">Choose your route</p>
        <h2
          class="courses-bundle__section-heading"
          id="courses-bundle-section-heading"
        >
          Pick the structured route that matches what you need next
        </h2>
      </div>

      <div class="courses-bundle__panel" data-course-tabs>
        <div class="courses-bundle__section-intro">
          <p class="section-label section-label--secondary">
            Why this offer works
          </p>
          <p class="courses-bundle__section-body">
            AI makes it easier to generate code, but not to judge tradeoffs,
            debug bad assumptions, or connect the stack end to end. These
            courses are built to strengthen the fundamentals and decision-making
            that still matter.
          </p>
          <p class="courses-bundle__section-body">
            If you want free, article-led orientation, start with the <a
              href="/guides">guides</a
            >. Choose a course when you want a more structured route through the
            material, with 1:1 support included and the option to build from one
            course to the full bundle.
          </p>
        </div>

        <div
          class="courses-bundle__tab-list"
          role="tablist"
          aria-label="Course purchase options"
        >
          <button
            type="button"
            id="courses-bundle-tab"
            class="courses-bundle__tab is-active"
            role="tab"
            aria-selected="true"
            aria-controls="courses-bundle-panel"
            tabindex="0"
            data-course-tab="bundle"
          >
            Full bundle
          </button>
          <button
            type="button"
            id="course-catalogue-tab"
            class="courses-bundle__tab"
            role="tab"
            aria-selected="false"
            aria-controls="course-catalogue"
            tabindex="-1"
            data-course-tab="catalogue"
          >
            Course catalogue
          </button>
        </div>

        <div
          class="courses-bundle__tab-panel"
          id="courses-bundle-panel"
          role="tabpanel"
          aria-labelledby="courses-bundle-tab"
          tabindex="0"
          data-course-panel="bundle"
        >
          <div class="courses-bundle__panel-grid">
            <div class="courses-bundle__intro">
              <p class="courses-bundle__kicker">Full bundle</p>
              <div class="courses-bundle__chips" aria-label="Bundle details">
                <span class="courses-bundle__chip courses-bundle__chip--status">
                  {courseLaunchLabel}
                </span>
                <span class="courses-bundle__chip">4 courses</span>
                <span class="courses-bundle__chip">Self-paced</span>
                <span class="courses-bundle__chip"
                  >{courseSupportOffer.bundleShort}</span
                >
              </div>
              <h3 class="courses-bundle__title" id="courses-bundle-heading">
                {courseBundle.title}
              </h3>
              <p class="courses-bundle__body">{courseBundle.summary}</p>
              <ul class="courses-bundle__benefits" role="list">
                {courseBundle.benefits.map((benefit) => <li>{benefit}</li>)}
              </ul>
              <div class="courses-bundle__actions">
                <a
                  href="/contact?reason=course-enquiry#contact-form"
                  class="button is-ink is-medium"
                >
                  Ask about the full bundle
                </a>
                <a
                  href="#course-catalogue"
                  class="courses-bundle__link"
                  data-course-switch="catalogue"
                >
                  Open the course catalogue
                </a>
              </div>
            </div>

            <div class="courses-bundle__aside page-panel page-panel--secondary">
              <p class="courses-bundle__aside-label">Good fit for</p>
              <ul class="courses-bundle__audience" role="list">
                {courseBundle.audience.map((item) => <li>{item}</li>)}
              </ul>
              <div class="courses-bundle__support">
                <p class="courses-bundle__support-label">Included support</p>
                <p class="courses-bundle__support-copy">
                  {courseSupportOffer.singleDetail}
                </p>
                <p class="courses-bundle__support-copy">
                  {courseSupportOffer.bundleDetail}
                </p>
                <p class="courses-bundle__support-copy">
                  {courseSupportOffer.additionalSessionsDetail}
                </p>
              </div>
            </div>

            <div class="courses-bundle__course-grid">
              {
                courseOffers.map((course) => (
                  <a
                    class="courses-bundle__course-card"
                    href={`#course-${course.slug}`}
                    aria-label={`Show ${course.title} in the course catalogue`}
                    data-course-switch="catalogue"
                  >
                    <div class="courses-bundle__course-header">
                      <span
                        class="courses-bundle__course-icon"
                        aria-hidden="true"
                        set:html={getCourseIcon(course.slug, 18)}
                      />
                      <div class="courses-bundle__course-heading">
                        <p class="courses-bundle__course-focus">
                          {course.focus}
                        </p>
                        <h4 class="courses-bundle__course-title">
                          {course.title}
                        </h4>
                      </div>
                    </div>
                    <p class="courses-bundle__course-copy">{course.teaser}</p>
                  </a>
                ))
              }
            </div>
          </div>
        </div>

        <div
          class="courses-bundle__tab-panel courses-bundle__tab-panel--catalogue"
          id="course-catalogue"
          role="tabpanel"
          aria-labelledby="course-catalogue-tab"
          tabindex="0"
          data-course-panel="catalogue"
          hidden
        >
          <div class="courses-bundle__catalogue-intro">
            <p class="section-label section-label--secondary">
              Course catalogue
            </p>
            <h3 class="course-catalogue__heading" id="course-catalogue-heading">
              Four focused courses you can take on their own or as one connected
              bundle
            </h3>
            <p class="courses-bundle__catalogue-body">
              Choose the one that matches the gap you need to close now, or use
              the bundle if you want the whole stack to connect from foundations
              to delivery.
            </p>
          </div>
          <div class="course-catalogue__grid">
            {
              courseOffers.map((course) => (
                <article class="course-card" id={`course-${course.slug}`}>
                  <div class="course-card__top">
                    <div class="course-card__header">
                      <span
                        class="course-card__icon"
                        aria-hidden="true"
                        set:html={getCourseIcon(course.slug, 20)}
                      />
                      <div class="course-card__heading">
                        <p class="course-card__eyebrow">{course.focus}</p>
                        <h3 class="course-card__title">{course.title}</h3>
                      </div>
                    </div>
                    <p class="course-card__status">{courseLaunchLabel}</p>
                  </div>
                  <p class="course-card__summary">{course.summary}</p>

                  <div class="course-card__details">
                    <div class="course-card__detail-block">
                      <p class="course-card__label">Topics</p>
                      <ul class="course-card__list" role="list">
                        {course.topics.map((topic) => (
                          <li>{topic}</li>
                        ))}
                      </ul>
                    </div>

                    <div class="course-card__detail-block">
                      <p class="course-card__label">You will leave with</p>
                      <ul class="course-card__list" role="list">
                        {course.outcomes.map((outcome) => (
                          <li>{outcome}</li>
                        ))}
                      </ul>
                    </div>
                  </div>

                  <a
                    href="/contact?reason=course-enquiry#contact-form"
                    class="course-card__cta"
                  >
                    Ask about this course
                  </a>
                </article>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </section>

  <section
    class="section courses-model flow-section flow-section--loose"
    aria-labelledby="courses-model-heading"
  >
    <div class="container is-max-desktop">
      <p class="section-label section-label--secondary">Why this format</p>
      <h2 class="courses-model__heading" id="courses-model-heading">
        Why this setup fits how people actually learn now
      </h2>
      <div class="courses-model__grid">
        {
          learningModel.map((item) => (
            <article class="courses-model__card">
              <div class="courses-model__card-header">
                <span
                  class="courses-model__card-icon"
                  aria-hidden="true"
                  set:html={item.icon}
                />
                <h3 class="courses-model__card-title">{item.title}</h3>
              </div>
              <p class="courses-model__card-body">{item.body}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section
    class="section course-support flow-section"
    aria-labelledby="course-support-heading"
  >
    <div class="container is-max-desktop">
      <p class="section-label section-label--secondary">1:1 support</p>
      <h2 class="course-support__section-heading" id="course-support-heading">
        1:1 that feels usable, not token
      </h2>
      <article
        class="course-support__panel course-support__panel--solo page-panel page-panel--secondary"
      >
        <p class="course-support__kicker">Included support</p>
        <p class="course-support__body">
          Having taught at boot camps, I know one of the hardest things is
          giving each student the attention they need at the exact moment they
          are stuck. The recorded material and notes here make self-paced study
          possible, and the included 1:1 support is there to restore that
          missing attention without forcing everyone through the same timetable.
        </p>
        <div class="course-support__included-grid" role="list">
          {
            supportInclusions.map((item) => (
              <div class="course-support__included-card" role="listitem">
                <div class="course-support__included-header">
                  <span
                    class="course-support__included-icon"
                    aria-hidden="true"
                    set:html={item.icon}
                  />
                  <p class="course-support__included-title">{item.title}</p>
                </div>
                <p class="course-support__included-body">{item.body}</p>
              </div>
            ))
          }
        </div>
        <ul class="course-support__list" role="list">
          {supportMoments.map((item) => <li>{item}</li>)}
        </ul>
        <a
          href="/contact?reason=course-enquiry#contact-form"
          class="course-support__link"
        >
          Ask about the included support pack
        </a>
      </article>
    </div>
  </section>

  <section
    class="section course-interest flow-section flow-section--loose"
    aria-labelledby="course-interest-heading"
  >
    <div class="container is-max-desktop">
      <div class="course-interest__panel page-panel page-panel--secondary">
        <div class="course-interest__copy">
          <p class="course-interest__kicker">Next step</p>
          <h2 class="course-interest__title" id="course-interest-heading">
            If this feels like the right fit, join the launch list or ask about
            enrolment
          </h2>
          <p class="course-interest__body">
            If you want this mix of self-paced material and targeted 1:1
            support, leave your email for launch updates, pricing, and earliest
            enrolment details, or send a message directly.
          </p>
          <div class="course-interest__actions">
            <a
              href="/contact?reason=course-enquiry#contact-form"
              class="button is-ink is-medium"
            >
              Send a message
            </a>
          </div>
        </div>
        <div class="course-interest__form-wrap">
          <CourseInterestForm
            idPrefix="courses-interest"
            source="courses-page"
            buttonLabel="Join the launch list"
            fineprint="Launch updates, first access, and earliest enrolment details only."
            successMessage="You're on the launch list. I'll email you when founding enrolment opens."
            ariaLabel="Register interest for courses from the courses page"
            stackFields
          />
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<script is:inline>
  const courseTabGroups = document.querySelectorAll("[data-course-tabs]");

  const getCourseTabFromHash = () => {
    const hash = window.location.hash.slice(1);

    if (hash === "course-catalogue" || hash.startsWith("course-")) {
      return "catalogue";
    }

    return null;
  };

  courseTabGroups.forEach((group) => {
    const tabs = Array.from(group.querySelectorAll("[role='tab']"));
    const panels = Array.from(group.querySelectorAll("[role='tabpanel']"));
    const switches = Array.from(group.querySelectorAll("[data-course-switch]"));

    const activateTab = (tabName, { focus = false } = {}) => {
      tabs.forEach((tab) => {
        const active = tab.dataset.courseTab === tabName;

        tab.setAttribute("aria-selected", String(active));
        tab.tabIndex = active ? 0 : -1;
        tab.classList.toggle("is-active", active);

        if (active && focus) {
          tab.focus();
        }
      });

      panels.forEach((panel) => {
        panel.hidden = panel.dataset.coursePanel !== tabName;
      });
    };

    const activateTabByIndex = (index) => {
      const targetTab = tabs[(index + tabs.length) % tabs.length];

      if (!targetTab?.dataset.courseTab) {
        return;
      }

      activateTab(targetTab.dataset.courseTab, { focus: true });
    };

    tabs.forEach((tab, index) => {
      tab.addEventListener("click", () => {
        if (tab.dataset.courseTab) {
          activateTab(tab.dataset.courseTab);
        }
      });

      tab.addEventListener("keydown", (event) => {
        switch (event.key) {
          case "ArrowRight":
          case "ArrowDown":
            event.preventDefault();
            activateTabByIndex(index + 1);
            break;
          case "ArrowLeft":
          case "ArrowUp":
            event.preventDefault();
            activateTabByIndex(index - 1);
            break;
          case "Home":
            event.preventDefault();
            activateTabByIndex(0);
            break;
          case "End":
            event.preventDefault();
            activateTabByIndex(tabs.length - 1);
            break;
        }
      });
    });

    switches.forEach((trigger) => {
      trigger.addEventListener("click", () => {
        const targetTab = trigger.getAttribute("data-course-switch");

        if (targetTab) {
          activateTab(targetTab);
        }
      });
    });

    const initialTab =
      getCourseTabFromHash() ??
      tabs.find((tab) => tab.getAttribute("aria-selected") === "true")?.dataset
        .courseTab ??
      tabs[0]?.dataset.courseTab;

    if (initialTab) {
      activateTab(initialTab);
    }

    if (window.location.hash && getCourseTabFromHash()) {
      const target = document.getElementById(window.location.hash.slice(1));

      if (target) {
        requestAnimationFrame(() => {
          target.scrollIntoView({ block: "start" });
        });
      }
    }

    window.addEventListener("hashchange", () => {
      const hashTab = getCourseTabFromHash();

      if (hashTab) {
        activateTab(hashTab);
      }
    });
  });
</script>

<style lang="scss">
  .courses-model,
  .courses-bundle,
  .course-support,
  .course-interest {
    padding-top: 0;
    padding-bottom: 0;
  }

  .course-interest {
    padding-bottom: var(--space-section-tight);
    margin-bottom: var(--space-section-tight);
  }

  .courses-bundle__section-heading,
  .courses-model__heading,
  .course-support__section-heading,
  .course-catalogue__heading {
    margin: 0 0 1.5rem;
    font-family: "Barlow Condensed", sans-serif;
    font-size: clamp(1.8rem, 4vw, 2.75rem);
    line-height: 1.06;
    color: var(--text-primary);
  }

  .courses-model__grid,
  .courses-bundle__panel-grid,
  .courses-bundle__course-grid,
  .course-catalogue__grid {
    display: grid;
    gap: 1rem;
  }

  @media (min-width: 768px) {
    .courses-model__grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .courses-bundle__course-grid,
    .course-catalogue__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  .courses-model__card {
    position: relative;
    overflow: hidden;
    padding: 1.45rem 1.3rem;
    border: 1px solid
      color-mix(in srgb, var(--accent-secondary) 14%, var(--border-subtle));
    border-radius: var(--radius-panel);
    background:
      radial-gradient(
        circle at 100% 0%,
        color-mix(in srgb, var(--accent-secondary) 12%, transparent) 0%,
        transparent 42%
      ),
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--surface-elevated) 94%, white) 0%,
        var(--surface-elevated) 100%
      ),
      var(--surface-elevated);
    box-shadow: 0 14px 30px rgba(var(--shadow-rgb), 0.05);

    &::before {
      content: "";
      position: absolute;
      top: 0;
      left: 1.3rem;
      width: 3.5rem;
      height: 2px;
      background: linear-gradient(
        90deg,
        var(--accent-secondary) 0%,
        transparent 100%
      );
      pointer-events: none;
    }
  }

  .courses-model__card-title,
  .courses-bundle__title,
  .course-card__title,
  .course-interest__title {
    margin: 0;
    font-family: "Barlow Condensed", sans-serif;
    line-height: 1.05;
    color: var(--text-primary);
  }

  .courses-model__card-title {
    font-size: 1.15rem;
  }

  .courses-model__card-header {
    display: flex;
    align-items: flex-start;
    gap: 0.85rem;
  }

  .courses-model__card-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.35rem;
    height: 2.35rem;
    border-radius: var(--radius-pill);
    background: color-mix(
      in srgb,
      var(--surface-page) 82%,
      var(--accent-secondary) 18%
    );
    color: var(--accent-secondary);
    flex-shrink: 0;

    :global(svg) {
      display: block;
    }
  }

  .courses-model__card-body,
  .courses-bundle__section-body,
  .courses-bundle__catalogue-body,
  .courses-bundle__body,
  .course-card__summary,
  .course-support__body,
  .course-interest__body {
    margin: 0.7rem 0 0;
    color: var(--text-muted);
    line-height: 1.7;
  }

  .courses-bundle__panel {
    padding: clamp(1.45rem, 3vw, 2.25rem);
    border: 1px solid
      color-mix(in srgb, var(--accent-primary) 14%, var(--border-subtle));
    border-radius: var(--radius-panel-large);
    background:
      radial-gradient(
        ellipse 62% 74% at 0% 0%,
        color-mix(in srgb, var(--accent-primary) 10%, transparent) 0%,
        transparent 72%
      ),
      radial-gradient(
        ellipse 54% 62% at 100% 0%,
        color-mix(in srgb, var(--accent-secondary) 12%, transparent) 0%,
        transparent 74%
      ),
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--surface-elevated) 96%, white) 0%,
        var(--surface-elevated) 100%
      ),
      var(--surface-elevated);
    box-shadow: 0 22px 48px rgba(var(--shadow-rgb), 0.08);
  }

  .courses-bundle__section-header {
    max-width: 38rem;
  }

  .courses-bundle__section-intro {
    max-width: 45rem;
  }

  .courses-bundle__section-body {
    max-width: 38rem;
  }

  .courses-bundle__tab-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.65rem;
    margin-top: 1.25rem;
    padding: 0.4rem;
    border: 1px solid color-mix(in srgb, var(--accent-primary) 12%, transparent);
    border-radius: calc(var(--radius-soft) + 0.25rem);
    background: color-mix(
      in srgb,
      var(--surface-page) 86%,
      var(--accent-primary) 14%
    );
  }

  .courses-bundle__tab {
    appearance: none;
    border: 0;
    border-radius: var(--radius-pill);
    min-height: 3rem;
    padding: 0.7rem 1rem;
    background: transparent;
    font-family: "Barlow Condensed", sans-serif;
    font-size: 0.95rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--text-muted);
    cursor: pointer;
    transition:
      background 180ms ease,
      color 180ms ease,
      box-shadow 180ms ease,
      transform 180ms ease;

    &:hover,
    &:focus-visible {
      color: var(--text-primary);
    }

    &:focus-visible {
      outline: 2px solid color-mix(in srgb, var(--accent-primary) 55%, white);
      outline-offset: 2px;
    }

    &[aria-selected="true"] {
      background: linear-gradient(
        135deg,
        color-mix(in srgb, var(--accent-primary) 96%, white) 0%,
        color-mix(in srgb, var(--accent-secondary) 74%, var(--accent-primary))
          100%
      );
      color: var(--text-on-strong);
      box-shadow: 0 12px 24px rgba(var(--shadow-rgb), 0.14);
    }
  }

  .courses-bundle__tab-panel {
    margin-top: 1.25rem;
  }

  .courses-bundle__tab-panel[hidden] {
    display: none;
  }

  .courses-bundle__panel-grid {
    display: grid;
    gap: 1.25rem;
  }

  .courses-bundle__catalogue-intro {
    max-width: 45rem;
  }

  .courses-bundle__kicker,
  .course-support__kicker,
  .course-interest__kicker,
  .course-card__eyebrow,
  .courses-bundle__course-focus,
  .courses-bundle__aside-label {
    margin: 0;
    font-family: "Barlow Condensed", sans-serif;
    font-size: 0.76rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .courses-bundle__kicker,
  .course-card__eyebrow,
  .courses-bundle__course-focus,
  .course-interest__kicker {
    color: var(--accent-primary);
  }

  .course-card__status {
    display: inline-flex;
    align-items: center;
    width: fit-content;
    min-height: 1.9rem;
    padding: 0.28rem 0.72rem;
    border-radius: var(--radius-pill);
    background: var(--accent-secondary);
    color: var(--text-on-strong);
    box-shadow: 0 10px 24px
      color-mix(in srgb, var(--accent-secondary) 22%, transparent);
  }

  .course-support__kicker,
  .courses-bundle__aside-label {
    color: var(--accent-secondary);
  }

  .courses-bundle__chips {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    margin: 0.9rem 0 1rem;
  }

  .courses-bundle__chip {
    display: inline-flex;
    align-items: center;
    min-height: 2rem;
    padding: 0.35rem 0.8rem;
    border-radius: var(--radius-pill);
    border: 1px solid color-mix(in srgb, var(--accent-primary) 18%, transparent);
    background: color-mix(
      in srgb,
      var(--accent-primary) 8%,
      var(--surface-elevated)
    );
    font-family: "Barlow Condensed", sans-serif;
    font-size: 0.76rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: color-mix(in srgb, var(--text-primary) 82%, var(--accent-primary));
  }

  .courses-bundle__chip--status {
    border-color: color-mix(in srgb, var(--accent-secondary) 16%, transparent);
    background: color-mix(
      in srgb,
      var(--accent-secondary) 14%,
      var(--surface-elevated)
    );
    color: color-mix(in srgb, var(--text-primary) 84%, var(--accent-secondary));
  }

  .courses-bundle__title,
  .course-interest__title {
    font-size: clamp(1.7rem, 4vw, 2.5rem);
  }

  .courses-bundle__benefits,
  .courses-bundle__audience,
  .course-card__list,
  .course-support__list {
    list-style: none;
    margin: 1rem 0 0;
    padding: 0;
    display: grid;
    gap: 0.75rem;
  }

  .courses-bundle__benefits li,
  .courses-bundle__audience li,
  .course-card__list li,
  .course-support__list li {
    position: relative;
    padding-left: 1rem;
    color: var(--text-primary);
    line-height: 1.65;

    &::before {
      content: "";
      position: absolute;
      top: 0.72rem;
      left: 0;
      width: 0.4rem;
      height: 0.4rem;
      border-radius: 50%;
      background: var(--accent-secondary);
      transform: translateY(-50%);
    }
  }

  .courses-bundle__actions,
  .course-interest__actions {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.25rem;
  }

  .courses-bundle__link,
  .course-support__link,
  .course-card__cta {
    font-family: "Barlow Condensed", sans-serif;
    font-size: 0.88rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
  }

  .courses-bundle__actions .courses-bundle__link {
    display: inline-flex;
    align-items: center;
    min-height: 2.85rem;
  }

  .courses-bundle__link,
  .course-card__cta {
    color: var(--accent-primary);
  }

  .course-support__link {
    color: var(--accent-secondary);
  }

  .course-support__panel--solo {
    margin-top: 0;
  }

  .course-support__link {
    display: inline-flex;
    align-items: center;
    margin-top: 1rem;
  }

  .courses-bundle__course-grid {
    margin-top: 0;
  }

  .course-catalogue__grid {
    margin-top: 1.5rem;
  }

  .courses-bundle__support {
    margin-top: 1.25rem;
    padding: 1rem;
    border: 1px solid
      color-mix(in srgb, var(--accent-secondary) 14%, transparent);
    border-radius: var(--radius-soft);
    background: color-mix(
      in srgb,
      var(--surface-page) 90%,
      var(--accent-secondary) 10%
    );
  }

  .courses-bundle__support-label {
    margin: 0;
    font-family: "Barlow Condensed", sans-serif;
    font-size: 0.76rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent-secondary);
  }

  .courses-bundle__support-copy {
    margin: 0.55rem 0 0;
    color: var(--text-primary);
    line-height: 1.6;
  }

  .course-support__included-grid {
    display: grid;
    gap: 0.8rem;
    margin-top: 1rem;
  }

  .course-support__included-card {
    padding: 0.95rem 1rem;
    border: 1px solid
      color-mix(in srgb, var(--accent-secondary) 14%, var(--border-subtle));
    border-radius: var(--radius-soft);
    background: color-mix(
      in srgb,
      var(--surface-page) 90%,
      var(--accent-secondary) 10%
    );
  }

  .course-support__included-header {
    display: flex;
    align-items: center;
    gap: 0.7rem;
  }

  .course-support__included-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: var(--radius-pill);
    background: color-mix(
      in srgb,
      var(--surface-page) 82%,
      var(--accent-secondary) 18%
    );
    color: var(--accent-secondary);
    flex-shrink: 0;

    :global(svg) {
      display: block;
    }
  }

  .course-support__included-title {
    margin: 0;
    font-family: "Barlow Condensed", sans-serif;
    font-size: 0.98rem;
    line-height: 1.08;
    color: var(--text-primary);
  }

  .course-support__included-body {
    margin: 0.65rem 0 0;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .courses-bundle__course-card,
  .course-support__panel--solo {
    position: relative;
    overflow: hidden;
  }

  .courses-bundle__course-card {
    display: flex;
    flex-direction: column;
    min-height: 100%;
    padding: 1rem;
    border: 1px solid color-mix(in srgb, var(--border-subtle) 84%, transparent);
    border-radius: var(--radius-soft);
    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--surface-elevated) 94%, white) 0%,
        var(--surface-elevated) 100%
      ),
      var(--surface-elevated);
    box-shadow: 0 14px 30px rgba(var(--shadow-rgb), 0.05);
    color: inherit;
    text-decoration: none;
    transition:
      transform 180ms ease,
      border-color 180ms ease,
      box-shadow 180ms ease;

    &:hover,
    &:focus-visible {
      transform: translateY(-2px);
      border-color: color-mix(
        in srgb,
        var(--accent-primary) 18%,
        var(--border-subtle)
      );
      box-shadow: 0 14px 28px rgba(var(--shadow-rgb), 0.08);
    }
  }

  .courses-bundle__course-header {
    display: flex;
    align-items: center;
    gap: 0.85rem;
  }

  .courses-bundle__course-heading {
    display: grid;
    gap: 0.25rem;
  }

  .courses-bundle__course-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.35rem;
    height: 2.35rem;
    border-radius: var(--radius-pill);
    background: color-mix(
      in srgb,
      var(--surface-page) 82%,
      var(--accent-primary) 18%
    );
    color: var(--accent-primary);
    flex-shrink: 0;

    :global(svg) {
      display: block;
    }
  }

  .courses-bundle__course-header .courses-bundle__course-title {
    margin: 0;
  }

  .courses-bundle__course-title {
    margin: 0.35rem 0 0;
    font-family: "Barlow Condensed", sans-serif;
    font-size: 1.05rem;
    line-height: 1.08;
    color: var(--text-primary);
  }

  .courses-bundle__course-copy {
    margin: 0.55rem 0 0;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .course-card {
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    scroll-margin-top: 6rem;
    min-height: 100%;
    padding: 1.55rem 1.45rem 1.4rem;
    border: 1px solid
      color-mix(in srgb, var(--accent-primary) 10%, var(--border-subtle));
    border-radius: var(--radius-panel);
    background:
      radial-gradient(
        circle at 100% 0%,
        color-mix(in srgb, var(--accent-primary) 10%, transparent) 0%,
        transparent 42%
      ),
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--surface-elevated) 95%, white) 0%,
        var(--surface-elevated) 100%
      ),
      var(--surface-elevated);
    box-shadow: 0 14px 30px rgba(var(--shadow-rgb), 0.05);

    &::before {
      content: "";
      position: absolute;
      top: 0;
      left: 1.45rem;
      width: 3.6rem;
      height: 2px;
      background: linear-gradient(
        90deg,
        var(--accent-primary) 0%,
        transparent 100%
      );
      pointer-events: none;
    }
  }

  .course-card__status {
    margin: 0;
    justify-self: end;
  }

  .course-card__top {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    grid-template-areas: "header status";
    align-items: flex-start;
    gap: 0.8rem 1rem;
  }

  .course-card__header {
    grid-area: header;
    display: flex;
    align-items: flex-start;
    gap: 0.9rem;
    min-width: 0;
  }

  .course-card__heading {
    display: grid;
    gap: 0.25rem;
  }

  .course-card__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.6rem;
    height: 2.6rem;
    border-radius: var(--radius-pill);
    background: color-mix(
      in srgb,
      var(--surface-page) 80%,
      var(--accent-primary) 20%
    );
    color: var(--accent-primary);
    flex-shrink: 0;

    :global(svg) {
      display: block;
    }
  }

  .course-card__title {
    font-size: 1.35rem;
  }

  .course-card__details {
    display: grid;
    gap: 1rem;
  }

  .course-card__label {
    margin: 0;
    font-family: "Barlow Condensed", sans-serif;
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent-secondary);
  }

  .course-card__cta {
    margin-top: auto;
  }

  .course-interest__panel {
    display: grid;
    gap: 1.25rem;
  }

  .course-interest__form-wrap {
    width: 100%;
  }

  @media (min-width: 960px) {
    .courses-bundle__panel-grid {
      grid-template-columns: minmax(0, 1.18fr) minmax(18rem, 0.82fr);
      align-items: start;
      gap: 1.35rem;
    }

    .courses-bundle__course-grid {
      grid-column: 1 / -1;
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .course-catalogue__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .course-support__included-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .course-interest__panel {
      grid-template-columns: minmax(0, 1.1fr) minmax(19rem, 0.9fr);
      align-items: center;
      gap: 1.5rem;
    }
  }

  @media (max-width: 479px) {
    .course-card__top {
      grid-template-columns: 1fr;
      grid-template-areas:
        "status"
        "header";
    }
  }
</style>
